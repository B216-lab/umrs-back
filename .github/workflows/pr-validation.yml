name: PR Validation

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  pr-title:
    uses: B216-lab/.github/.github/workflows/pr-title-validation.yml@main

  docker-smoke:
    uses: ./.github/workflows/reusable-docker-smoke.yml
